# frozen_string_literal: true
# Gem file created by `bundle init`

source "https://rubygems.org"

# gem "rails"


gem 'activerecord', '~> 7.0' # Manage ORM

gem 'pg', '~> 1.5' # PostgreSQL adapter

# `standalone_migrations` gem handle DB migrations w/ rails
# uses `rake` tasks to provide rails-like modules db:create, db:migrate etc
gem 'standalone_migrations'

group :development do
    gem 'pry' # debugging
end
